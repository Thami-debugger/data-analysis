{
  "version": 2,
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python",
      "config": {
        "runtime": "python3.11",
        "postInstallScript": "find . -type d -name 'tests' -exec rm -rf {} + && find . -type d -name '__pycache__' -exec rm -rf {} + && find . -name '*.dist-info' -exec rm -rf {} + && find . -name '*.egg-info' -exec rm -rf {} +"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
